# myopiadetection
Notice: Copy code into your IDE (Ex. VSCode or Cursor) and follow these steps.

Steps to start Myopia detection:

(Terminal one)
cd "C:\YOURDIRECTORY\myopiacode"
uvicorn yoloapi:app --reload --host 0.0.0.0 --port 8000


(Terminal two)
cd "C:\YOURDIRECTORY\myopiacode\frontend"
npm run dev
